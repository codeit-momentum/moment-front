import styled from 'styled-components';

export const BackBtnField = styled.div`
  display: flex;
  justify-content: flex-start;
  align-items: center;
  width: 100%;
  padding: 1rem; // 적절한 여백 추가
`;

export const BackBtnWrapper = styled.button`
  background: none;
  border: none;
  padding: 0;
  cursor: pointer;
  display: flex;
  justify-content: center;
  align-items: center;
`;
