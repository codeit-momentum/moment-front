import mixin from './mixin';

const colors = {
  white: '#FCFCFC',
  black: '#020202',
  gray: '#DBDBDB',
  darkGray: '#999999',
  yellow: '#FAED46',
  blue: '#6A7CB7',
  red: '#FF0000',
};

const theme = { mixin, colors };

export default theme;
